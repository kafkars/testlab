schema_version = 165
id = "kafka.admin-create-partitions-manual-replica-placement"
title = "packaged admin expands a topic with exact replica placement"
description = "The packaged public admin expands a one-partition topic with caller-ordered assignments for two new partitions, and immediate independent metadata requires the exact complete partition set, replica order, replica leader, and full ISR."
timeout_ms = 90000
requires = ["admin", "lifecycle", "client_readiness"]
assertions = []

[[steps]]
id = "create-client"
kind = "create_client"
client_id = "client-1"

[[steps]]
id = "await-client-ready"
kind = "await_client_ready"
client_id = "client-1"

[[steps]]
id = "create-topic"
kind = "create_topic"
client_id = "client-1"
operation_id = "admin-create-topic"
topic = "testlab-kafkars-admin-manual-partition-expansion"
partitions = 1
replication_factor = 2
validate_only = false
timeout_ms = 30000

[[steps]]
id = "create-manually-placed-partitions"
kind = "create_partitions"
client_id = "client-1"
operation_id = "admin-create-manually-placed-partitions"
topic = "testlab-kafkars-admin-manual-partition-expansion"
total_count = 3
replica_assignments = [[3, 1], [2, 3]]
validate_only = false
expected_current_count = 1
timeout_ms = 30000

[[steps]]
id = "shutdown-client"
kind = "shutdown_client"
client_id = "client-1"
