schema_version = 117
id = "kafka.admin-describe-metadata-quorum"
title = "packaged admin describes independently observed metadata quorum"
description = "The packaged client's public metadata-quorum result must preserve canonical leader, epoch, watermark, voter, observer, directory, timestamp, and optional controller-listener state consistent with immediate status and replication snapshots from Kafka's pinned quorum CLI."
timeout_ms = 60000
requires = ["admin", "lifecycle", "client_readiness"]
assertions = []

[[steps]]
id = "create-client"
kind = "create_client"
client_id = "client-1"

[[steps]]
id = "await-client-ready"
kind = "await_client_ready"
client_id = "client-1"

[[steps]]
id = "describe-metadata-quorum"
kind = "describe_metadata_quorum"
client_id = "client-1"
operation_id = "admin-describe-metadata-quorum"
timeout_ms = 20000

[[steps]]
id = "shutdown-client"
kind = "shutdown_client"
client_id = "client-1"
