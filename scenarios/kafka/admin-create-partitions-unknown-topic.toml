schema_version = 37
id = "kafka.admin-create-partitions-unknown-topic"
title = "packaged admin rejects partition creation for an unknown Kafka topic"
description = "The packaged client's public admin requests a partition-count increase for an absent topic, reports the exact normalized unknown-topic error, and an immediate independent metadata query confirms the topic remains absent."
timeout_ms = 90000
requires = ["admin", "lifecycle", "client_readiness"]
assertions = []

[[steps]]
id = "create-client"
kind = "create_client"
client_id = "client-1"

[[steps]]
id = "await-client-ready"
kind = "await_client_ready"
client_id = "client-1"

[[steps]]
id = "create-partitions-unknown-topic"
kind = "create_partitions"
client_id = "client-1"
operation_id = "admin-partitions-unknown-topic"
topic = "testlab-kafkars-admin-partitions-missing"
total_count = 2
validate_only = false
expected_error_code = "broker:broker_3"
timeout_ms = 20000

[[steps]]
id = "shutdown-client"
kind = "shutdown_client"
client_id = "client-1"
