schema_version = 37
id = "kafka.admin-list-offsets-invalid-partition"
title = "packaged admin rejects offset listing for an absent Kafka partition"
description = "The packaged client's public admin requests the latest offset for absent partition 1 of an independently provisioned one-partition topic, reports the exact normalized unknown-topic-or-partition error, and an immediate independent metadata query confirms only partition 0 exists."
timeout_ms = 90000
requires = ["admin", "lifecycle", "client_readiness"]
assertions = []

[[steps]]
id = "create-client"
kind = "create_client"
client_id = "client-1"

[[steps]]
id = "await-client-ready"
kind = "await_client_ready"
client_id = "client-1"

[[steps]]
id = "list-offsets-invalid-partition"
kind = "list_offsets"
client_id = "client-1"
operation_id = "admin-list-offsets-invalid-partition"
topic = "testlab-kafkars-admin-offsets-invalid-partition"
partition = 1
position = "latest"
expected_error_code = "broker:broker_3"
timeout_ms = 20000

[[steps]]
id = "shutdown-client"
kind = "shutdown_client"
client_id = "client-1"
