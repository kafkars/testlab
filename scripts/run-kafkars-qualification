#!/usr/bin/env bash
set -euo pipefail

repository_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
source "$repository_root/scripts/portable-environment"
evidence_directory="${TESTLAB_EVIDENCE_DIR:-${repository_root}/target/kafkars-qualification-evidence}"

cd "${repository_root}"
cargo build -p testctl -p testlab-kafkars-adapter
target/debug/testctl qualify \
  --root . \
  --qualification qualifications/kafkars-pr.toml \
  --subject subjects/kafkars-rust.toml \
  --evidence-dir "${evidence_directory}"
