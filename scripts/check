#!/usr/bin/env bash
set -euo pipefail
repo_root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
cd "$repo_root"
zrail check
"$repo_root/scripts/check-rust-lint"
"$repo_root/scripts/check-rust-test"
"$repo_root/scripts/check-scenarios"
"$repo_root/scripts/run-reference-qualification"
